apiVersion: apps/v1
kind: Deployment
metadata:
  name: {{ include "flask-web-chart.fullname" . }}
spec:
  replicas: {{ .Values.replicaCount }}
  selector:
    matchLabels:
      app: {{ include "flask-web-chart.fullname" . }}
  template:
    metadata:
      labels:
        app: {{ include "flask-web-chart.fullname" . }}
    spec:
      containers:
        - name: flask-web
          image: "{{ .Values.image.repository }}:{{ .Values.image.tag }}"
          imagePullPolicy: {{ .Values.image.pullPolicy }}
          ports:
            - containerPort: {{ .Values.service.port }}
